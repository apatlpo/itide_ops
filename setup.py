#!/usr/bin/env python

from distutils.core import setup

#INSTALL_REQUIRES = ['xarray >= 0.10.6', ]

setup(name='itide',
      description='Various tools for internal tide extraction experiments',
      url='https://github.com/apatlpo/itide_ops',
      packages=['itide'])
